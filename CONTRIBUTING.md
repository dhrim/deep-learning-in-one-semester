# 이 저장소에 손대실 때

노트북을 고치거나 새로 만드실 때 지킬 것들입니다.
**책에는 없습니다.** 책은 읽는 사람을 위한 것이고, 이 문서는 저장소를
건드리는 사람을 위한 것입니다.

읽기만 하실 거라면 [README](README.md) 로 가십시오.

---

## 1. 노트북의 표준 구성

노트북 105개가 하나의 틀을 따릅니다.

```
① 제목 (markdown)      — 무엇을 보이려는 노트북인가
② 준비 (markdown)
③ 부트스트랩 (code)    — dlbook 설치
④ 셋업 (code)          — import, 시드, 버전 출력
⑤ 데이터 (markdown+code)
⑥ 모델 정의 (markdown+code)   ★ 판마다 다른 유일한 셀
⑦ 실험 (markdown+code) — 여러 개 가능
⑧ 정리 (markdown)      — 표 + 요지 + 연습
```

**⑥만 판마다 다릅니다.** 나머지는 세 판이 글자 하나까지 같습니다.
학생이 세 판을 나란히 열면 **차이가 나는 셀이 곧바로 눈에 띄는 것**이
이 저장소의 교육적 장치입니다. 그 장치를 깨지 마십시오.

### ③ 부트스트랩 — 모든 노트북 공통

```python
try:
    import dlbook
except ImportError:
    !pip install -q "dlbook @ git+https://github.com/dhrim/deep-learning-in-one-semester.git"
    import dlbook
```

`try/except` 로 감싸는 이유는 이미 깔린 환경에서 재설치를 피하기
위해서입니다. 재설치하면 커널을 다시 시작해야 할 수 있습니다.

### ④ 셋업 — 모든 노트북 공통

```python
import numpy as np
import matplotlib.pyplot as plt

import dlbook
from dlbook import data, metrics, plot

dlbook.set_seed(42)
plot.use_korean()
print(dlbook.versions())
```

`dlbook.versions()` 를 찍는 이유는 **노트북에 실행 결과를 담아 커밋하기
때문**입니다. 저장된 결과가 어느 버전에서 나온 것인지 그 줄이 답합니다.

---

## 2. 어디서 열어도 돌아가야 합니다

`tools/lint_notebooks.py` 가 검사합니다. 규칙 번호(R1~R8)는 그 파일 안에서만
쓰는 이름입니다. 책에는 같은 규칙이 번호 없이 실려 있습니다.

| 이렇게 | 왜 |
|---|---|
| `google.colab` 을 import 하지 않는다 | Colab 밖에서 죽습니다 |
| 드라이브 마운트를 하지 않는다 | 플랫폼마다 다릅니다 |
| 절대 경로(`/content`, `/kaggle`, `C:\`, `/Users/`)를 쓰지 않는다 | 플랫폼마다 다릅니다 |
| `pip install` 은 첫 코드 셀에서만 | 중간에 설치하면 이미 불러온 모듈과 충돌합니다 |
| 데이터는 `dlbook.data` 로만 얻는다 | 로컬 우선 탐색이 여기 들어 있습니다 |
| 시드를 `dlbook.set_seed()` 로 고정한다 | 두 번 돌려 다른 값이 나오면 학생이 판단을 못 합니다 |
| 수치는 `dlbook.record()` 로 남긴다 | 출력에 이름이 붙어 남습니다 |
| 한글 폰트는 `plot.use_korean()` 으로 | 플랫폼마다 폰트가 다릅니다 |
| 모델은 `.keras` 로 저장한다 | Keras 3에서 `.h5` 는 옛 형식입니다 |

**pip 규칙을 어기면 학생 절반이 막힙니다.** numpy 계열이 특히 그렇습니다.

---

## 3. 노트북 하나는 10분을 넘지 않습니다

실습 60분 안에 이것이 들어가야 하기 때문입니다.

1. 그대로 한 번 돌린다 (10분)
2. 숫자를 하나 바꿔 다시 돌린다 (20분)
3. 결과를 옆 사람과 견준다 (15분)
4. 정리 (15분)

**3번이 핵심입니다.** 학생들이 서로 다른 값을 얻는 경험은 강의로는
만들어지지 않습니다.

실측으로 38개 중 37개가 7분 안에 끝납니다. `ch08_datasets` 하나만
8~11분이고, **그것은 의도한 것이라 줄이지 않습니다.** CIFAR-10이 이만큼
걸린다는 것도 겪어 봐야 할 사실입니다.

시간을 줄여야 한다면 이 순서로 보십시오 — 데이터 개수, epoch,
모델 크기. 결론이 바뀌지 않는 선까지만 줄입니다.

주차별 실행 시간은 책 부록 D(강의 운영)에 표로 있습니다.

---

## 4. 실행 결과를 담아서 커밋합니다

저장소를 열어 본 사람이 아무것도 설치하지 않고 결과를 볼 수 있어야 합니다.
GitHub이 `.ipynb` 를 그대로 렌더링하므로, 출력이 담겨 있으면 링크 하나로
실습 전체가 보입니다. **교재를 고르는 강의자에게 이것이 전부입니다.**

```bash
python tools/run_notebooks.py notebooks/keras --write-back
```

- `--write-back` 은 실행된 결과를 노트북 파일에 저장합니다.
- `--skip-executed` 는 이미 결과가 담긴 것을 건너뜁니다(중단된 갱신 이어하기).
- 저장할 때 **실행기가 남긴 잡음**(oneDNN 안내, 커널 경고)만 걷어냅니다.
  코드가 낸 출력은 경고든 오류든 건드리지 않습니다.

**언제 다시 담나** — 출간 직전, 프레임워크 버전을 올렸을 때, 실습을 크게
고쳤을 때. 그 외에는 이유가 없습니다. 딥러닝은 돌릴 때마다 값이 조금씩
달라서, 자주 다시 담으면 git 히스토리만 무거워집니다.

GitHub Actions 의 `refresh` 작업(`workflow_dispatch`)이 이 일을 대신합니다.

---

## 5. 스모크 모드

PR마다 105개를 전부 돌리면 너무 오래 걸립니다. CI는 **이번 커밋에서 바뀐
노트북만** 골라, **축소 모드**로 돌립니다.

```bash
python tools/run_notebooks.py notebooks/keras --smoke
```

`DLBOOK_SMOKE=1` 이 켜지면 `dlbook.smoke.epochs()` 가 1을 돌려주고
`dlbook.smoke.subset()` 이 데이터를 1/20로 줄입니다.
**노트북 쪽에서 이 둘을 쓰셔야 스모크가 먹습니다.**

```python
m.fit(x, y, epochs=dlbook.smoke.epochs(30), ...)
```

**`--smoke` 와 `--write-back` 은 함께 쓸 수 없습니다.** 축소 실행의 결과를
교재에 담아 두면 거짓말이 됩니다. 도구가 막습니다.

---

## 6. 수치에는 이름을 붙입니다

```python
acc = metrics.accuracy(y_test, pred)
dlbook.record("ch11_lstm_acc", acc)      # 출력: ch11_lstm_acc = 0.9123
```

본문이 인용한 숫자가 **어느 노트북의 어느 줄에서 나왔는지** 이름으로
찾아갑니다. 그리고 그 줄이 노트북 출력에 그대로 남아 있습니다.

이름 규칙은 `ch<장번호>_<무엇>_<지표>` 입니다.

**세 판의 값은 같지 않습니다.** 파라미터를 세는 방식과 초기값 분포가
프레임워크마다 다릅니다(예: LSTM 파라미터 Keras 5,497 대 PyTorch 5,625 —
PyTorch는 편향이 두 벌). 노트북의 「정리」 표를 쓰실 때는 **그 판에서 실제로
나온 값**을 적으십시오. 한 판의 값을 세 판에 돌려 쓰면 거짓이 됩니다.

> 예전에는 이 값들을 `expected.json` 에 모아 CI가 매주 대조했습니다.
> 폐기했습니다. 딥러닝은 돌릴 때마다 값이 달라 가짜 경보만 냈고,
> 이제는 노트북에 결과가 담겨 있어 열어 보면 됩니다.
> 그때 쓰던 실측값 중 본문 그림 다섯 장이 쓰는 것만
> `figures/measured.json` 으로 남겼습니다.

---

## 7. 세 판의 절 구성이 같아야 합니다

```bash
python tools/check_parity.py
```

Keras 3 판이 정본입니다. TensorFlow 판과 PyTorch 판은 **같은 파일 이름**으로
존재해야 하고, **마크다운 제목이 같아야** 합니다.

**한 판만 고치면 안 됩니다.** 세 판을 나란히 놓고 읽는 것이 이 저장소의
장치인데, 한쪽만 조용히 달라지면 그 장치가 무너집니다.

노트북은 대부분 `tools/gen/gen_chNN.py` 가 만듭니다. **생성기가 있는 노트북은
직접 고치지 마시고 생성기를 고친 뒤 다시 만드십시오.** 그래야 세 판이 함께
바뀝니다.

```bash
DLREPO=$(pwd) python tools/gen/gen_ch04.py
```

---

## 8. 그림 규칙

그림 45장은 전부 `figures/make_figures.py` 가 만듭니다.
**손으로 그린 파일은 없습니다.**

| | |
|---|---|
| **SVG로만 낸다** | 본문과 웹이 같은 파일을 씁니다. 두 벌을 유지하면 반드시 어긋납니다 |
| **인용 그림을 쓰지 않는다** | 논문이나 다른 책의 그림을 가져오지 않습니다. 저작권 협의가 필요 없습니다 |
| **색만으로 구분하지 않는다** | 흑백 인쇄를 전제합니다. 점은 마커 모양(● / ▲), 선은 선 종류(실선 / 파선), 영역은 명도 차를 함께 바꿉니다 |
| **팔레트를 새로 만들지 않는다** | 파일 위쪽의 색 상수만 씁니다. 한 그림에 강조는 한 곳입니다 |
| **파일 이름이 자리를 말한다** | `fig_<장>_<번호 두 자리>_<내용>.svg` |
| **본문 형식은 하나다** | 캡션은 「그림 N-M. 」으로 시작해 마침표로 끝내고, id는 `{#fig-N-MM}`(번호 두 자리), 폭은 `width=76%` 기본, 경로는 `../figures/` |
| **캡션은 독자에게 하는 말이다** | ✗ 「세 겹의 동심원. 바깥부터 AI / ML / DL」 ✓ 「그림 1-1. 딥러닝은 머신러닝의 일부이고, 머신러닝은 인공지능의 일부다.」 |
| **한 장에 두세 개** | 표로 될 것을 그림으로 그리지 마십시오. **말로 하면 세 문장이 넘는가** — 넘으면 그림입니다 |

새 그림은 ① 함수를 쓰고 ② 만들고 ③ 원고에 링크, 순서로 넣습니다.
**③을 먼저 하면** 파일이 없는 채로 링크가 걸립니다.

---

## 9. 새 장을 만들 때의 순서

이 책이 실제로 따른 순서입니다.

1. **실험을 먼저 돌립니다.** 노트북이 아니라 스크립트로 빠르게.
2. **수치를 확인합니다.** 시드 여러 개, 학습률 여러 개.
3. **생성기를 씁니다** (`tools/gen/gen_chNN.py`).
4. **세 판을 만들고 lint · parity 를 통과시킵니다.**
5. **전부 실행하고 그 결과를 노트북에 담아 커밋합니다** (`--write-back`).
6. **그 수치로 원고를 씁니다.**
7. **마지막에 그림을 얹습니다.**

**6번이 원고인 것이 핵심입니다.** 원고를 먼저 쓰면 숫자를 맞추고 싶어집니다.
2번에서 초고가 여러 번 뒤집혔습니다.

---

## 10. PR을 주시면

CI가 자동으로 —

1. 위 규칙들을 검사하고 (`lint_notebooks.py`)
2. 세 판의 절 구성이 어긋나지 않았는지 보고 (`check_parity.py`)
3. **이번 커밋에서 바뀐 노트북만** 축소 실행합니다

셋 다 통과해야 병합됩니다. 정기 실행은 없습니다 — 버전이 `==` 로 고정돼
있어 아무도 손대지 않은 코드가 저절로 깨질 일이 없습니다.
